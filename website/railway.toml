[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "web"

[services.variables]
NODE_ENV = "production"
PORT = "3000"

[[services.ports]]
port = 3000
handler = "http"

[[services.ports.http]]
path = "/"

[[services.health_checks]]
path = "/"
interval = "15s"
timeout = "10s"
retries = 5
