[phases.setup]
nixPkgs = ["...", "nodejs_20", "nodePackages.npm"]

[phases.install]
cmds = [
  "npm ci"
]

[phases.build]
cmds = [
  "npm run prisma:generate",
  "npm run prisma:deploy"
]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
PORT = "8080"
